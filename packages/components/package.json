{
  "name": "@nighttrax/components",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "compile:paths": "bob-ts -i src --paths true",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "peerDependencies": {
    "react": "~16.13.1"
  },
  "dependencies": {
    "@nighttrax/foo": "*"
  },
  "devDependencies": {
    "@types/react": "~16.9.43",
    "typescript": "~4.5.2",
    "bob-ts": "^1.2.2",
    "bob-tsm": "^0.3.6"
  }
}
